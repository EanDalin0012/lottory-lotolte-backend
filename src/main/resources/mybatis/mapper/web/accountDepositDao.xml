<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lattory.lattoryLotoBackEnd.web.dao.AccountDepositDao">

    <insert id="save">
        <![CDATA[
            INSERT INTO deposit
            (
                id
                , account_id
                , account_id_refer
                , to_account_id
                , to_account_id_refer
                , amount
                , currency
                , deposit_by
                , deposit_at
                , remark
            ) VALUES (
                #{id}
                , #{accountID}
                , #{accountIDRefer}
                , #{toAccountID}
                , #{toAccountIDRefer}
                , #{amount}
                , #{currency}
                , #{userID}
                , to_char(NOW(), 'YYYY-MM-DD hh:mm:ss')
                , #{remark}
            )
    	]]>
    </insert>


    <select id="count" resultType="java.lang.Integer">
        <![CDATA[
		  SELECT
	     	COUNT (*)
	      FROM "deposit"
	     ]]>
    </select>


</mapper>