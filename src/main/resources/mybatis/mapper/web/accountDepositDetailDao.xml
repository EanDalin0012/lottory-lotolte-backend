<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lattory.lattoryLotoBackEnd.web.dao.AccountDepositDetailDao">

    <insert id="save">
        <![CDATA[
            INSERT INTO account_deposit_detail
            (
                id
                , deposit_id
                , account_id
                , current_account_balance
                , to_account_id
                , to_current_account_balance
                , deposit_amount
                , currency
                , date
                , by
            ) VALUES (
                #{id}
                , #{depositId}
                , #{accountId}
                , #{currentAccountBalance}
                , #{toAccountId}
                , #{toCurrentAccountBalance}
                , #{amount}
                , #{currency}
                , to_char(NOW(), 'YYYY-MM-DD hh:mm:ss')
                , #{userID}
            )
    	]]>
    </insert>


    <select id="count" resultType="java.lang.Integer">
        <![CDATA[
		  SELECT
	     	COUNT (*)
	      FROM "account_deposit_detail"
	     ]]>
    </select>


</mapper>